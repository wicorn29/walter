<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>wicorn29 gitstrap</title>
</head>
<body>
    gitstrap (walter) <br>
    
    
    <br><br>
    <progress></progress>
    <br><br>
    if you are reading this, something probably went wrong.<br>
    please check your internet connection, and if the issue persists<br>
    get a new version of this gitstrap build from wicorn29

    <script>
        function warn(msg) {
            console.warn(msg);
            alert(msg);
        }

        if (!navigator.onLine) {
            warn("check your internet connection");
        } else {
            fetch("https://raw.githubusercontent.com/wicorn29/walter/refs/heads/main/client/main.wui")
                .then(r => {
                    if (!r.ok) throw new Error("fetch failed");
                    return r.text();
                })
                .then(html => {
                    document.open();
                    document.write(html);
                    document.close();
                })
                .catch(() => {
                    warn("The server has moved. Please get a new version of the launcher from wicorn29.");
                });
        }
    </script>
</body>
</html>
